"use client";import {useState} from 'react';
export default function ContactForm(){const [message,setMessage]=useState('');const [sent,setSent]=useState(false);async function submit(e){e.preventDefault();const form=new FormData(e.currentTarget);const response=await fetch('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});const result=await response.json();if(response.ok){setSent(true);setMessage('Thanks — your message was sent.');e.currentTarget.reset();}else setMessage(result.error);}return <form className="contact-form" onSubmit={submit}><div className="form-two"><label className="field"><span>Name</span><input name="name" required maxLength="100"/></label><label className="field"><span>Email</span><input name="email" type="email" required maxLength="200"/></label></div><label className="field"><span>Subject</span><input name="subject" maxLength="200"/></label><label className="field"><span>Message</span><textarea name="message" required maxLength="3000"/></label><label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex="-1" autoComplete="off"/></label>{message&&<p role="status" className={sent?'form-success':'form-message'}>{message}</p>}<button className="button no-margin">Send message</button></form>}
