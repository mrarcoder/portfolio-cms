status: production_deployed_awaiting_owner_setup
current_phase: 7
implemented:
  - Next.js public portfolio, project details, setup/login, protected admin, same-origin API proxy, metadata, sitemap, and robots.
  - Cloudflare Worker authentication, content API, D1 persistence, R2 media validation/serving, contact limits, and inbox.
  - Profile and all accepted content CRUD, visibility, ordering, uploads, appearance settings, and dashboard counts.
  - Fresh-install, deployment, development, contribution, scope, architecture, and API documentation.
  - Futuristic glass interface with responsive admin navigation, modal content editors, toast feedback, subtle motion, and reduced-motion support.
  - Header inbox with unread count, responsive mail-style message filters and reply flow, and category selection for skills.
  - Project cards contain long content on narrow screens, and social links show automatically resolved site favicons with an initial fallback.
  - Admin content cards use shrink-safe text and action regions, with stacked project rows at constrained dashboard widths.
  - Frontend code is organized by admin, auth, portfolio, and shared UI domains; helper and stylesheet folders follow the same boundaries.
  - AI references, specifications, workflows, progress, and contributor guidance are indexed under docs/.
  - Public navigation is generated from the same visibility and content conditions as every rendered portfolio section, including responsive access on mobile.
  - Root hydration tolerates browser-extension attributes on the html element while preserving mismatch checks inside the application tree.
  - The admin profile editor shows the active portrait and supports full viewing, replacement, 4:5 cropping, zoom, position, rotation, brightness, and contrast adjustments before upload.
  - Admin action controls use consistent SVG icons with accessible labels and hover titles across content, profile, settings, inbox, and sign-out workflows.
  - Vercel Web Analytics is integrated at the root layout for route-aware production page-view tracking.
completed:
  - User accepted the reduced V1 scope.
  - Created YAML specifications for scope, boundaries, architecture, data/API, and delivery order.
  - Implemented all six local roadmap phases without adding deferred features.
  - Rehearsed setup and every main workflow against a fresh isolated D1 and R2 state through the Next.js proxy.
  - Provisioned production D1 portfolio-cms and private R2 portfolio-cms-media, applied all migrations, configured Worker secrets, and deployed portfolio-cms-api.
  - Created the Vercel portfolio-cms project, configured WORKER_API_URL, and deployed the production frontend at https://portfolio-cms-rho-woad.vercel.app.
checks:
  - All project YAML files parse successfully.
  - Every file reference in docs/AGENTS.yml resolves.
  - ESLint, focused Node tests, Worker dry-run bundle, and optimized Next.js production build pass.
  - End-to-end smoke covers singleton setup, PBKDF2 login, session cookie, uploads, CRUD, ordering, hidden media, section visibility, contact inbox, rendered pages, and logout.
  - Desktop and mobile visual checks cover the inbox list, unread badge, message detail popup, reply action, and skill category selector.
  - Full-page mobile portfolio and desktop social-link editor checks cover project containment and live logo preview.
  - Fresh-state production smoke verifies all 18 concrete page routes, the API proxy, every content resource, metadata routes, authentication, media, inbox, and the not-found boundary after restructuring.
  - Documentation YAML parses and every docs/AGENTS.yml file reference resolves after the documentation move.
  - Fresh-state smoke asserts that each enabled portfolio navigation link has a rendered target and disabled sections are omitted.
  - Desktop and narrow-screen browser checks cover the current-photo card, full preview, crop controls, adjusted WebP export, upload, profile update, replaced-file cleanup, and active-image reload.
  - Admin icon controls pass lint/build checks and desktop browser inspection across content editing, profile actions, inbox actions, and sidebar sign-out.
  - Production Worker health, Vercel deployment readiness, same-origin /api/health proxying, public/setup/login routes, and incomplete first-owner setup state are verified.
blockers:
  - The owner must choose the production administrator username and password through /setup.
  - The Vercel account must authorize the GitHub repository before automatic deployments from main can be enabled; CLI deployment remains available.
next_action: Complete first-owner setup, delete the one-time SETUP_TOKEN, connect GitHub in Vercel, and verify authenticated production workflows.
decisions:
  - One Next.js application and one Worker; no monorepo packages.
  - One theme; no blog, theme ecosystem, or import/export in V1.
  - Use same-origin API forwarding so browser sessions work on provider subdomains.
  - Protect initial setup with a deployment token and singleton database constraint.
  - Prefer fresh server reads before introducing caching.
maintenance: Update this file after each implementation task; record actual checks and unresolved issues, not intended results.
