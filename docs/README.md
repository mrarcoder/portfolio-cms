# Project documentation

Use these references according to the task:

| File | Purpose |
| --- | --- |
| `AGENTS.yml` | AI workflow, boundaries, and required reading |
| `SCOPE.yml` | Accepted product scope and exclusions |
| `PROGRESS.yml` | Implemented work, checks, blockers, and next action |
| `ARCHITECTURE.yml` | System boundaries and code organization |
| `DATA_API.yml` | Data model, API behavior, and security rules |
| `ROADMAP.yml` | Delivery phases and completion order |
| `DEVELOPMENT.yml` | Local development workflow |
| `DEPLOYMENT.yml` | Cloudflare and frontend deployment workflow |
| `CONTRIBUTING.md` | Contribution checks and repository rules |

Start with `AGENTS.yml`, then read only the reference relevant to the current task.
